import { ProjectsConsole } from "@/components/project/ProjectsConsole";
import { getAllProjects, getFeaturedProjects } from "@/lib/content";
export const metadata = { title: "Projects" };
export default function ProjectsPage() { const projects = getAllProjects(); const featured = getFeaturedProjects(); return <section className="page-console"><div className="container"><header className="page-title"><p>PROJECT REGISTRY / ALL PUBLIC REPOSITORIES</p><h1>项目档案</h1><span>重点项目 {String(featured.length).padStart(2, "0")} 个 · 自动同步 GitHub 公开仓库</span></header><section className="featured-strip">{featured.map((project) => <div key={project.repoName}><span>CH-{String(project.featured).padStart(2, "0")}</span><b>{project.frontmatter.title}</b><small>{project.category}</small></div>)}</section><ProjectsConsole projects={projects} /></div></section>; }
